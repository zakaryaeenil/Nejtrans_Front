import { RapportLineHelper } from './rapport-line-helper';

describe('RapportLineHelper', () => {
  it('should create an instance', () => {
    expect(new RapportLineHelper()).toBeTruthy();
  });
});
